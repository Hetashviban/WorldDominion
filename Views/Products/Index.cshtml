@model IEnumerable<WorldDominion.Models.Product>

@{
    ViewData["Title"] = "Index";
}

<h1>Stock Up on Essentials</h1>

<p class="products-new">
    @if (User.IsInRole("Admin"))
    {
        <a asp-action="Create" class="back">Create New</a>
    }
</p>

<partial name="~/Views/Shared/Products/_ProductsTablePartial.cshtml" model="Model" />